import css from './styles.module.css';

const Header = () => {
  return (
      <div className={css.title}>
        <h1 className={css.titleContent}>starships store</h1>
      </div>
  )
}

export default Header;
